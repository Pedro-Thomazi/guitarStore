import styles from './SearchPage.module.css'

import { Link } from 'react-router-dom'

// import { eletricGuitars, acousticGuitars, allGuitars } from '../../data/guitars'

const SearchPage = () => {
  // const [searchParams] = useSearchParams()

  // const url = [eletricGuitars, acousticGuitars]

  return (
    <div className={styles.container}>
        <h1>Pagina ainda não funciona.</h1>
        <Link to='/'>Voltar</Link>
    </div>
  )
}

export default SearchPage